#include <gtest/gtest.h>

TEST(OfferFilter, CanQueryEndpoint)
{

  // TODO: wait for the mesos api to become available, and
  // then hit the API with a filter, then attempt to provision
  // an application on that host using marathon; finally,
  //

}


